services:
#  postgres:  # Uncomment everything under postgres if you require internal postgres service, make sure to also uncommment "depends on" below
#    image: postgres:15-alpine
#    container_name: postgres_container
#    environment:
#      POSTGRES_USER: ${POSTGRES_USER:-postgres}
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#      POSTGRES_DB: ${POSTGRES_DB:-raman:research_prod}
#      PGDATA: /var/lib/postgresql/data/pgdata
#    volumes:
#      - ./postgres_data:/var/lib/postgresql/data
#    networks:
#      - medical_network
#    restart: unless-stopped
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U postgres"]
#      interval: 10s
#      timeout: 5s
#      retries: 5

  web:
    build: .
    container_name: medical_web
    command: gunicorn --config gunicorn_config.py app:app
    environment:
      DB_NAME: ${DB_NAME:-raman_research_prod}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_HOST: ${DB_HOST:-postgres_container}
      DB_PORT: ${DB_PORT:-5432}
      SECRET_KEY: ${SECRET_KEY}
      FLASK_ENV: production
      BACKUP_DIR: ${BACKUP_DIR:-/mnt/medical_backups/raman_backups}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-90}
    ports:
      - "5000:5000"
    networks:
      - medical_network
    volumes:
      - .:/app
      - ./backups:/backups
      - ./uploads:/app/uploads
      - /mnt/medical_backups:/mnt/medical_backups
#    depends_on:  # Uncomment if using internal postgres service
#      postgres:
#        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:5000/health" ]
      interval: 30s
      timeout: 3s
      retries: 3

  nginx:
    image: nginx:alpine
    container_name: medical_nginx
    ports:
      - "80:80"
      # - "8443:443"  # Uncomment when SSL is configured
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # - ./ssl:/etc/nginx/ssl:ro  # Uncomment when you add SSL certificates
    depends_on:
      - web
    networks:
      - medical_network
    restart: unless-stopped

networks:
  medical_network:
    driver: bridge
